Node 80-18-44-f0-71-30 is at 250/250 pods (hard kubelet cap) from other
tenants' workloads (verified via `oc describe node`, Non-terminated Pods:
250 in total). Per the shared-cluster guardrails, I did not delete/evict
any pod I did not create to make room.

Effect: the flowlogs-pipeline DaemonSet (netobserv ns) and
netobserv-ebpf-agent DaemonSet (netobserv-privileged ns, auto-created by
the netobserv operator as its companion privileged namespace -- standard
operator behavior, not something applied directly) each have 1 pod
Pending, hard node-affinity-bound to 80-18-44-f0-71-30 specifically
(DaemonSet controller behavior), because that node cannot admit any more
pods:
  FailedScheduling: 0/3 nodes are available: 1 Too many pods,
  2 node(s) didn't satisfy plugin(s) [NodeAffinity]

Both DaemonSets ARE running (2/2 achievable replicas Ready) on the other
two nodes: 24-6e-96-33-24-90 and ec-f4-bb-ed-07-a8. All three demo-vms
VMs (vm-demo, vm-demo-2, vm-non-migratable) run on ec-f4-bb-ed-07-a8,
which DOES have a running agent + pipeline pod, so flow capture for the
demo-vms namespace is intact.

FlowCollector 'cluster' status.conditions[type=Ready].status = True
(reason "Ready,Degraded" -- degraded solely due to the one
capacity-blocked node above and a LokiStack replication-factor warning,
not a configuration or RBAC failure).
